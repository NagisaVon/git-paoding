# v0.1.2 release-readiness evidence

This note separates completed offline evidence from owner-gated live and release actions. It
must not be read as a tag, publication, or live-smoke record.

## Completed offline evidence

The explicit field-shaped performance test generates 315 Git directories, 33 changed files, 36
diff atoms, and seven semantic slices. The 2026-08-31 local run observed and asserted:

| Architectural measure | Observed | Required |
|---|---:|---:|
| Recursive full-tree reads | 2 | exactly 2 |
| `mktree` writes | 40 | at most the generated dirty-ancestor bound of 40 |
| `ls-remote` calls | 1 | exactly 1 |
| pushes | 1 | at most 1 |
| GitHub mutation readbacks | 0 | exactly 0 |

Wall-clock duration is printed by `scripts/benchmark_publish.py` for observation only and is
never an assertion. The performance marker is excluded from default pytest and must be selected
explicitly.

The behavioral traceability manifest still maps exactly twelve safety properties to twelve
stable, pytest-discoverable tests. Those properties cover single ownership, multiple slices per
file, idempotence, selective refresh, stable PR identity, metadata recovery, canonical isolation,
archive behavior, delete/add identity, the deliberate no-buildability rule, integration fidelity,
and preservation of human PR narrative bytes.

The accepted atomic capability evidence is
`docs/evidence/atomic-push-github-2026-08-31.json`. It records a successful GitHub two-ref atomic
create, a successful two-ref atomic update carrying one exact lease per destination, exact remote
OID verification, and cleanup with no remaining probe refs. Therefore the exact-lease atomic path
remains enabled and the per-slice fallback remains disabled.

## Pending owner-gated evidence

No field-shaped live release run or real `init --pr` smoke has been performed for this note. After
the owner authorizes one exact private scratch repository, the documented release-validation
command must create `docs/evidence/live-release-v0.1.2-2026-08-31.json`. That new evidence must
contain the real first-progress observation, eight phase timings, no-op republish proof with zero
pushes and zero PR edits, the real open integration PR used by `init --pr`, and any comparable
historical samples used to evaluate the non-blocking 3× preparation target.

CP6 sign-off, the release tag, trusted-publisher upload, and remote CI results remain pending until
that live evidence exists and the owner approves release. None of those actions is performed by
offline preparation.
